- name: Bash shell playbook
  hosts:
  - slightawx.uktme.cisco.com
  - anuwilliawx.uktme.cisco.com
  - ssharmanawx.uktme.cisco.com

  tasks:
  - name: Docker PS
    shell: "docker ps"
    register: command_result
    args:
      executable: /bin/bash

  - debug: msg={{ command_result.stdout }}
